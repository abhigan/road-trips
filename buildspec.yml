version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18.x

  pre_build:
    commands:
      - npm install

  build:
    commands:
      - npm run build

artifacts:
  files:
    - build/static/**/*
  name: builds/$CODEBUILD_BUILD_NUMBER/artifacts
